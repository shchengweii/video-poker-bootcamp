// ===================================================
// ============ Global Setups ========================
// ===================================================

// player to start with 100 points
let playerPoints = 100;

// player to start with bet 0;
let playerBet = 0;

// allow player to deal the card at the start
let canDeal = true;

// allow player to replace the card at the start
let canReplace = true;

// create an empty array to store the card deck
let cardDeck = [];

// create an empty array to store the cards deal for the first time
let playerHand = [];

// create an empty array to store the cards to keep after player 'hold' the cards
let cardToKeep = [];
